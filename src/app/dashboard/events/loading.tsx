import type { FunctionComponent } from 'react';
import { PageLoader } from '@components/ui';

const EventsLoading: FunctionComponent = () => {
    return <PageLoader/>;
};

export default EventsLoading;
